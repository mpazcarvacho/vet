<%= form_with(model: pet_history, local: true) do |form| %>
  <% if pet_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet_history.errors.count, "error") %> prohibited this pet_history from being saved:</h2>

      <ul>
      <% pet_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :weight %>
    <%= form.text_field :weight, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :height %>
    <%= form.text_field :height, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :diagnosis %>
    <%= form.text_area :diagnosis, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :pet_id %>
    <%= form.collection_select( :pet_id, @pets, :id, :name, {}, class: "form-control")%>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
